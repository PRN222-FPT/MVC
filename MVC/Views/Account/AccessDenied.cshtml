@{
    ViewData["Title"] = "Access Denied";
}

<div class="d-flex align-items-center justify-content-center" style="min-height: 70vh;">
    <div class="text-center">
        <span class="material-icons text-warning" style="font-size: 48px;">lock</span>
        <h1 class="h4 fw-bold mt-3">Access denied</h1>
        <p class="text-secondary mb-4">Your account does not have permission to open this page.</p>
        <a asp-controller="Document" asp-action="Library" class="btn btn-primary">Return to Library</a>
    </div>
</div>
